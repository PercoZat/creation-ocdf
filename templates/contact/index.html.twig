{% extends 'base.html.twig' %}

{% block title %}Page de contact{% endblock %}

{% block body %}
<main class="container">
    <h1 class="row justify-content-center">Contactez-nous !</h1>

    {{ form_start(contactForm) }}
        {{ form_widget(contactForm) }}
        <input type="submit" value="Envoyer" class="btn btn-lg btn-primary btn-block">
    {{ form_end(contactForm) }}
</main>
{% endblock %}